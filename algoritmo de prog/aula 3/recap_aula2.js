//variaveis e const
let variavelname=("nome random")
console.log(variavelname)

/**
 * padroes de escrita de variaveis
CAMEL CASE (usado em prog)
ex nomeComepletoTotal

SNAKE CASE (usado em bancos de dados)
ex nome_completo_total
*/

/**
int (numero inteiro)
float (numero real)
string (texto/caracteres)
boleau (logico-true/folse)
null (valor vazio)
undefined (n definido, quando cria a variavel)
*/

/** 
 * tipos de expressoes aritimértica (+, -, *, /, %)
 * + = soma
 * - = subtração 
 * * = multiplicação 
 * / = divisão
 * % = resto da divisão
 * 
 * tipo de expresão Lógica (<, >, <=, >=, ==, !=, ===, !==)
 * <, maior que
 * >, menor que 
 * >=, maior ou igua 
 * <=, menor ou igual
 * ==, igual a 
 * !=, diferente de 
 * ===, identico
 * !==,  
 * 
 * tipo de expresão q n lembro o nome (&&, ||, !)
 * && e
 * || ou
 * ! não
 */